import { describe, expect, it } from "vitest";
import { run } from "./run";

describe("job", () => {
  it("call", () => {
    // await expect(Promise.resolve(1)).resolves.toBe(1);
    run;
    // source('{}');
    // expect(source('{}').toSlug()).toEqual(source('{}').toSlug());
    // await expect(run(json(source("{}")))).resolves.toEqual({});
    // await expect(run(source('{}'))).resolves.toEqual('{}');
    // await expect(run(promise(Promise.resolve({id: 1})))).resolves.toEqual({id: 1})
  });
});

// describe('test', () => {
//   it('hi', () => {});
// })
